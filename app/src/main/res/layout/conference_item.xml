<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="item"
            type="com.survivalcoding.ifkakao.model.conferenceData.Data" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_marginBottom="20dp"
        android:background="@android:color/transparent">

        <FrameLayout
            android:id="@+id/frame_layout"
            android:layout_width="118dp"
            android:layout_height="66dp"
            android:layout_marginStart="15dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/thumbnail"
                android:layout_width="118dp"
                android:layout_height="66dp"
                android:contentDescription="@string/thumbnail"
                android:src="@drawable/ic_launcher_background"
                app:imageUrlWithRound="@{item.linkList.pcImage[0].url}" />

            <TextView
                android:id="@+id/running_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|end"
                android:background="@drawable/back_running_time_text_view"
                android:paddingStart="5dp"
                android:paddingEnd="5dp"
                android:textColor="@color/white"
                app:runningTime="@{item.linkList.video[0].description}" />

        </FrameLayout>


        <TextView
            android:id="@+id/field"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="30dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/outline_title_text_view"
            android:paddingStart="10dp"
            android:paddingTop="2dp"
            android:paddingEnd="10dp"
            android:paddingBottom="2dp"
            android:textColor="@color/white"
            android:textSize="15sp"
            app:field="@{item.field}"
            app:layout_constraintLeft_toRightOf="@id/frame_layout"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/title"
            android:layout_width="216dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="30dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/white"
            app:layout_constrainedWidth="true"
            app:layout_constraintLeft_toRightOf="@id/frame_layout"
            app:layout_constraintTop_toBottomOf="@id/field"
            app:title="@{item.title}" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>